# Production - pharmatracker.in
server {
    listen 80;
    server_name pharmatracker.in www.pharmatracker.in;
    
    # React Frontend (static files)
    location / {
        root /var/www/pharmatracker-in;
        try_files $uri $uri/ /index.html;
        index index.html;
    }
    
    # NestJS API - /api/* -> Port 3001
    location /api/ {
        proxy_pass http://localhost:3001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}

# Staging - staging.pharmatracker.in
server {
    listen 80;
    server_name staging.pharmatracker.in;
    
    # React Frontend (static files)
    location / {
        root /var/www/staging-pharmatracker-in;
        try_files $uri $uri/ /index.html;
        index index.html;
    }
    
    # NestJS API - /api/* -> Port 3000
    location /api/ {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}
